# 2026-03-29 日记

## A股风向标项目

### 前端修复
- **静态文件路径问题**：Flask `app/__init__.py` 中 `static_folder` 默认指向 `app/static/`，但实际静态文件在 `stock_app/static/`。在 `create_app()` 中显式指定了正确的 `static_folder` 路径。
- **pct_change 类型错误**：`market.js` 的 `renderLimitUpCard` 中 `x.pct_change.toFixed(2)` 报错——API 返回的 `pct_change` 是字符串（如 `"21.1900"`），加 `parseFloat()` 转换解决。
- 清理了调试用的 console.log，保留了 try/catch。

### 架构
- 运行目录：`/root/.openclaw/projects/stock_app/`（非 git 仓库）
- Git 仓库：`/root/.openclaw/workspace/stock_project/`
- 两个目录需要手动同步修改
- 服务端口：5001
- git push 到 GitHub 网络不通（GnuTLS recv error），commit 已完成待推送

### 追踪功能逻辑
- 表：`golden_cross_track`
- 追踪中的股票：顺灏股份(002565)、盈方微(000670)、卓胜微(300782)，从 3-20 开始（9天）；平安银行(000001)从 3-28 开始（1天）
- `add_track` 只插入 ts_code/trade_date/status，没有查 `stock_basic` 补全 name/close/ma5/ma20，刚添加的股票列表显示为空

### 前端各页
- 概况页 / 排行页：正常
- 板块页 / 追踪页：代码结构正常未实际验证

## 动植物图鉴

### 植物数据修复
- 文件：`/root/.openclaw/workspace/plants_data.json`
- 本次编辑因 JSON 编辑器问题产生冗余字节导致文件损坏，用 `git checkout` 恢复后用 Python 脚本重新添加
- 已提交 commit `87a065d`：新增金银忍冬(id=247)、樱桃李(id=248)
- 当前总数：185 种，无重复 id

### 金银忍冬 (id=247)
- 学名：Lonicera japonica，忍冬科忍冬属，半常绿缠绕藤本

### 樱桃李 (id=248)
- 学名：Prunus cerasifera，蔷薇科李属，落叶小乔木/灌木，原产中亚和西亚
